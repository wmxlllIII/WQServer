package com.example.test.server.mapper;

import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface OfflineMessageMapper {
//
//    @Insert("INSERT INTO offline_messages(user_id, content, status) VALUES(#{userId}, #{content}, #{status})")
//    @Options(useGeneratedKeys = true, keyProperty = "id")
//    void insert(OfflineMessage message);
//
//    @Select("SELECT * FROM offline_messages WHERE user_id = #{userId} AND status = 'pending'")
//    List<OfflineMessage> selectPendingMessages(String userId);
//
//    @Update("UPDATE offline_messages SET status = #{status} WHERE id = #{id}")
//    void updateStatus(@Param("id") Long id, @Param("status") String status);

}
